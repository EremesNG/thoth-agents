Codex-only generated artifact fixtures for the harness adapter MVP.

These fixtures intentionally cover representative Codex surfaces only:
agent TOML, project config TOML, MCP TOML, skill layout manifest, plugin package
manifest/provenance/hooks, and capability diagnostics. Do not add Claude,
Antigravity, or OpenCode outputs here.

Update these files only when the validated Codex surface contract or deterministic
writer output intentionally changes.
