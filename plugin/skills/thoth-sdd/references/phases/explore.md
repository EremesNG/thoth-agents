# Explore contract

**Owner**: explorer (read-only)<br>
**Applies to**: Full

Resolve only repository uncertainty that affects requirements or design. Inspect
the smallest useful anchors and return relevant paths/symbols, confirmed current
behavior, constraints, safe assumptions, unresolved human-owned decisions, and
recommended specification anchors. Do not write files or return raw logs. Stop
when root can specify without repeating discovery.
